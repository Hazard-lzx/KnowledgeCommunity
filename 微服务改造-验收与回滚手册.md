# 微服务改造 · 验收与回滚手册

> 完成日期：2026-09-06。本手册记录四步改造的验收结果与各级回滚杠杆。

## 一、当前架构

```
前端 (vite dev proxy → :9000)
        │
   Gateway :9000 (Spring Cloud Gateway + JWT 鉴权 + Redis 令牌桶限流 + traceId 注入)
        │
        ├── /api/ai/** /api/qa/**   ──直连──▶ ai-service :8000 (FastAPI + LangChain/LangGraph)
        ├── /api/auth|users|oss/**  ──lb://──▶ auth-service :8101
        ├── /api/articles/**        ──lb://──▶ article-service :8102
        ├── /api/search/**          ──lb://──▶ search-service :8103
        ├── /api/feed/**            ──lb://──▶ feed-service :8104
        └── /api/internal/**        ──404 封锁（仅服务间内网直连 + X-Internal-Token）
```

- 服务发现：Nacos 192.168.80.80:8848（4 个 Java 服务注册，healthy）
- 共享设施：MySQL（本机）、Redis / ES / Milvus / RocketMQ（192.168.80.80）
- 通信：业务接口走网关（JWT）；服务间走 Feign（Nacos lb + X-Internal-Token + Sentinel 熔断 + X-Trace-Id 透传）；MQ 链路：文章发布 → RocketMQ → article-service 消费 → ai-service（摘要生成 + Milvus 索引）
- 单体已退役：`backend/monolith` 代码保留、移出构建（父 pom modules 已注释）

## 二、分步验收结果

### 第 1 步：轻量网关 + 统一鉴权
| 验收标准 | 结果 |
|---|---|
| 全站功能经网关回归 | 通过（10/10 E2E） |
| 伪造 X-User-Id/X-Username/X-Internal-Token 头被剥离 | 通过 |
| 白名单接口可匿名访问 | 通过（/api/auth/**、/api/search/** 任意方法；GET /api/feed|articles|users/**） |
| 登出 token 拒绝（Redis token:valid:{jti} 白名单） | 通过 |

### 第 2 步：AI 出城（Python 直连）
| 验收标准 | 结果 |
|---|---|
| 三能力复刻：RAG 问答 / 写作助手 / Agent SSE | 通过（SSE 契约与单体一致，前端零改动） |
| Milvus 1024 维 collection（text-embedding-v3）+ 存量回填 | 通过（12 篇全量重嵌入） |
| MQ 索引链路（发布 → RocketMQ → ai-service → Milvus） | 通过 |
| Result<T> 契约 + 全局异常处理 | 通过 |
| 网关 AI 路由 11 分钟超时（> Agent 10 分钟上限） | 通过（660000ms） |
| 每用户配额限流（Python 内 Redis 计数）+ 网关令牌桶 | 通过 |

### 第 3 步：Nacos + 按域拆分
| 验收标准 | 结果 |
|---|---|
| 网关路由 lb:// 服务发现 | 通过（5 条路由，Nacos 4/4 healthy） |
| auth / article / search / feed 四服务拆分 | 通过（只信网关身份头 + X-Internal-Token，不解析 JWT） |
| 跨服务 Feign 契约（common DTO + Result 无参构造） | 通过（文章带作者信息、用户带文章统计双向验证） |
| 单体退役（移出构建） | 通过（8080 已停，代码保留） |
| 发布全链路（MQ 摘要回填 + ES 可搜） | 通过（articleId=35，10 秒内摘要回填） |
| 内部接口经网关 404 | 通过 |
| 鉴权链路 8 项冒烟 | 8/8 通过 |

### 第 4 步：收尾加固
| 验收标准 | 结果 |
|---|---|
| Sentinel 熔断（异常比例 >50% 熔断 10s，规则见 common SentinelRuleConfig） | 通过（6 个 Feign 资源，代码配置无 Dashboard 依赖） |
| fallbackFactory 降级：article-service 停机时 feed 返回 200 + 空列表 | 通过（ArticleClientFallbackFactory WARN 日志确认） |
| traceId 全链路：网关注入 → 四服务 MDC 日志 `[服务名,traceId]` → Feign/httpx 透传 → 响应头回传 | 通过（一次请求贯穿 feed/article/auth 同一 traceId） |
| Feign 超时治理（connect 2s / read 5s 快失败） | 通过（超时计入熔断异常比例） |
| Agent 工具回调收口（X-Internal-Token + X-User-Id + 15s 独立超时） | 通过（复核对齐，无需改动） |

## 三、回滚手册（按影响范围从小到大）

### R1. AI 链路回滚（Python → 单体旧 AI 模块）
- 前提：单体代码保留一个版本周期（当前已满足）
- 操作：`gateway/src/main/resources/application.yml` 中 ai-service 路由 uri 改回 `http://localhost:8080`，启动单体，重启 gateway
- 影响：仅 AI 三件套；Java 业务服务不动

### R2. 网关路由回滚（lb:// → 单体静态路由）
- 操作：gateway application.yml 删除 4 条 lb:// 路由，恢复 `monolith-default`（`http://localhost:8080`，Path=/api/**，置于 ai-service 路由之后）
- 同步：父 pom 恢复 `<module>monolith</module>` 注释，重新构建单体
- 影响：全部业务回到单体；新服务停机即可

### R3. 网关整体旁路（前端直连单体）
- 操作：`frontend/vite.config.js` dev proxy 从 9000 改回 8080
- 影响：失去统一鉴权/限流/traceId，仅应急使用

### R4. 单服务回滚（仅一个域异常）
- 例：feed-service 故障 → 网关删除 feed 路由，单体（含全部域）承接 /api/feed/**
- 前提：单体保留完整四域代码（当前满足），且与单体共用同一 MySQL/Redis

### 数据与中间件注意
- Milvus 向量数据独立于单体（单体旧 Redis 向量已废弃），回滚 R1 后 RAG 检索不可用
- MQ topic 消费组不冲突：单体与新服务同名消费组二选一运行，避免重复消费
- 新增测试数据（articleId 33-35、e2e-lbroute-smoke 用户）无需清理

## 四、运维速查

**启动顺序**：基础设施（MySQL/Redis/ES/Milvus/RocketMQ/Nacos）→ ai-service → auth(8101) → article(8102) → search(8103) → feed(8104) → gateway(9000)

```powershell
# ai-service
cd 'e:\Full-stack Project\Knowledge Community\ai-service'
python -W ignore -m uvicorn app.main:app --host 0.0.0.0 --port 8000

# Java 服务（backend 目录，dev profile 默认激活，敏感配置在 application-dev.yml / .env）
java -jar auth-service\target\auth-service-1.0.0.jar
java -jar article-service\target\article-service-1.0.0.jar
java -jar search-service\target\search-service-1.0.0.jar
java -jar feed-service\target\feed-service-1.0.0.jar
java -jar gateway\target\gateway-1.0.0.jar

# 全量构建（单体不参与）
mvn clean package -DskipTests -f backend\pom.xml
```

**冒烟脚本**（backend/scripts/）：
- `smoke_lb_auth.py` 鉴权链路 8 项（注册/登录/401/点赞/封锁）
- `smoke_sse_lb.py` SSE RAG 问答
- `smoke_publish_chain.py` 发布 → MQ 摘要 + 索引全链路

**排障**：任一响应头 `X-Trace-Id` → 在各服务日志 grep 同一 traceId 即可还原完整调用链。

## 五、超时治理台账

| 位置 | 配置 | 说明 |
|---|---|---|
| 网关默认路由 | 60s | 业务接口 |
| 网关 AI 路由 | 660s | > Agent 10 分钟上限，防长流式被掐断 |
| 网关连接 | 5s | connect-timeout |
| Feign（四服务统一） | connect 2s / read 5s | 快失败，超时计入 Sentinel 熔断 |
| Java→Python（MQ 消费） | connect 5s / request 120s | 摘要与索引调用（LLM 慢） |
| Python 工具回调（httpx） | 15s | 每工具独立短超时，防卡死循环 |
| Python LLM | chat 120s / tool 60s | deepseek-v3 |
| Redis | 3s | 全服务统一 |
